import { OPEN_TO, SKILLS } from "../../constants";
import { S } from "../../styles";
import { Badge, Reveal, SectionTag, SectionTitle } from "../ui";

export function AboutSection() {
  return <div style={S.sectionWrap} id="about"><div style={{ ...S.section, display: "grid", gridTemplateColumns: "1fr 1fr", gap: "6rem", alignItems: "start" }}><Reveal><SectionTag>{"// About"}</SectionTag><SectionTitle>I Fix Messy.<br />I Build Lasting.</SectionTitle><div style={{ marginTop: "2rem", display: "flex", flexDirection: "column", gap: "1.25rem", color: "hsl(var(--muted-foreground))", fontSize: "1.05rem", lineHeight: 1.8 }}><p>I&apos;m a Senior Frontend Engineer who specializes in <strong style={{ color: "hsl(var(--foreground))", fontWeight: 600 }}>frontend systems that stay clean as products scale.</strong></p><p>I&apos;ve shipped across <strong style={{ color: "hsl(var(--foreground))", fontWeight: 600 }}>e-commerce, fintech, transport, IoT,</strong> and large internal tools.</p><p>The goal is always the same: architecture that&apos;s invisible to users and frictionless for the team building it.</p></div><div style={{ marginTop: "2rem", display: "flex", flexWrap: "wrap", gap: "0.5rem" }}>{SKILLS.map((s) => <Badge key={s}>{s}</Badge>)}</div></Reveal><Reveal delay={150}><div style={{ borderLeft: "2px solid hsl(var(--accent))", paddingLeft: "1.75rem", fontFamily: "'Syne', sans-serif", fontSize: "1.35rem", fontWeight: 700, lineHeight: 1.45, letterSpacing: "-0.02em", color: "hsl(var(--foreground))", marginTop: "4rem" }}>&quot;Good frontend architecture should fade into the background for users.&quot;</div><div style={{ marginTop: "3rem", padding: "2rem", background: "hsl(var(--card))", border: "1px solid hsl(var(--border))", borderRadius: "var(--radius)" }}><p style={{ fontFamily: "'DM Mono',monospace", fontSize: "0.68rem", letterSpacing: "0.1em", textTransform: "uppercase", color: "hsl(var(--muted-foreground))", marginBottom: "1rem" }}>Currently open to</p><div style={{ display: "flex", flexDirection: "column", gap: "0.6rem" }}>{OPEN_TO.map((i) => <div key={i} style={{ display: "flex", alignItems: "center", gap: "0.75rem", fontSize: "0.9rem", color: "hsl(var(--foreground))" }}><span style={{ color: "hsl(var(--accent))", fontSize: "0.6rem" }}>◆</span>{i}</div>)}</div></div></Reveal></div></div>;
}
